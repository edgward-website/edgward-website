---
import Base from '../layouts/Base.astro';
import MediaCard from '../components/MediaCard.astro';
import SectionHeader from '../components/SectionHeader.astro';

const articles = [
  {
    mediaName: 'Tribune de Genève',
    date: 'Janvier 2025',
    excerpt: "Une startup genevoise veut démocratiser la sécurité privée en permettant à chacun de commander une patrouille comme on commande un taxi.",
    url: '#',
  },
  {
    mediaName: 'RTS',
    date: 'Décembre 2024',
    excerpt: "Edgward, la plateforme qui connecte particuliers et entreprises de sécurité, lève le voile sur son modèle communal testé à Soral.",
    url: '#',
  },
  {
    mediaName: '24heures',
    date: 'Novembre 2024',
    excerpt: "La commune vaudoise de Soral fait figure de pionnière en subventionnant pour ses habitants un abonnement à la sécurité à la demande.",
    url: '#',
  },
  {
    mediaName: 'Léman Bleu',
    date: 'Octobre 2024',
    excerpt: "Reportage sur l'essor des services de sécurité à la demande en Suisse romande. Edgward, au cœur du sujet.",
    url: '#',
  },
  {
    mediaName: 'Le Temps',
    date: 'Septembre 2024',
    excerpt: "L'économie à la demande s'invite dans la sécurité. Analyse du modèle Edgward et de son impact sur les entreprises de surveillance traditionnelles.",
    url: '#',
  },
  {
    mediaName: 'PME Magazine',
    date: 'Août 2024',
    excerpt: "Edgward se positionne comme une alternative crédible aux contrats de télésurveillance classiques pour les PME romandes.",
    url: '#',
  },
];
---

<Base
  title="Espace presse — Edgward dans les médias"
  description="Retrouvez toutes les apparitions d'Edgward dans la presse. Tribune de Genève, RTS, 24heures, Léman Bleu et plus encore."
>
  <!-- HERO -->
  <section class="hero">
    <div class="container">
      <div class="hero-inner">
        <span class="hero-eyebrow">Espace presse</span>
        <h1>Edgward dans <em>les médias</em></h1>
        <p>Retrouvez nos apparitions dans la presse romande et les sujets qui parlent d'Edgward.</p>
      </div>
    </div>
  </section>

  <!-- MEDIA CARDS -->
  <section class="section media-section">
    <div class="container">
      <SectionHeader
        eyebrow="Revue de presse"
        title="Ils parlent de <em>nous</em>"
      />
      <div class="media-grid">
        {articles.map(article => (
          <MediaCard
            mediaName={article.mediaName}
            date={article.date}
            excerpt={article.excerpt}
            url={article.url}
          />
        ))}
      </div>
    </div>
  </section>

  <!-- PRESS CONTACT -->
  <section class="section press-contact-section">
    <div class="container">
      <div class="press-block fade-in">
        <h3>Contact presse</h3>
        <p>Pour toute demande d'interview, de visuels ou d'information supplémentaire :</p>
        <a href="mailto:presse@edgward.ch" class="press-email">presse@edgward.ch</a>
        <div class="press-assets">
          <span class="assets-label">Ressources disponibles :</span>
          <div class="assets-tags">
            <span class="tag">Logo Edgward (SVG / PNG)</span>
            <span class="tag">Photos de presse</span>
            <span class="tag">Dossier de presse PDF</span>
            <span class="tag">Biographies fondateurs</span>
          </div>
        </div>
      </div>
    </div>
  </section>
</Base>

<style>
  .hero {
    background: var(--navy); color: white;
    padding: 72px 0 56px; text-align: center;
    position: relative; overflow: hidden;
  }
  .hero::before {
    content: ''; position: absolute;
    top: -200px; left: 50%; transform: translateX(-50%);
    width: 500px; height: 500px;
    background: radial-gradient(circle, var(--brand-glow) 0%, transparent 60%);
    pointer-events: none;
  }
  .hero-inner { position: relative; z-index: 1; max-width: 560px; margin: 0 auto; }
  .hero-eyebrow { display: inline-block; font-size: 12px; color: var(--brand-light); font-weight: 700; text-transform: uppercase; letter-spacing: 0.12em; margin-bottom: 16px; }
  .hero h1 { font-size: clamp(34px, 4vw, 50px); font-weight: 800; line-height: 1.1; letter-spacing: -0.025em; margin-bottom: 16px; color: white; }
  .hero h1 em { font-family: 'Instrument Serif', Georgia, serif; font-style: italic; font-weight: 400; color: var(--brand-light); }
  .hero p { font-size: 17px; color: #cbd5e1; line-height: 1.6; }

  .media-section { background: white; }
  .media-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }

  /* PRESS CONTACT */
  .press-contact-section { background: var(--soft); }
  .press-block {
    max-width: 600px; margin: 0 auto;
    background: white; border: 1px solid var(--border);
    border-radius: var(--radius-lg); padding: 36px;
    text-align: center;
  }
  .press-block h3 { font-size: 20px; font-weight: 700; color: var(--navy); margin-bottom: 8px; }
  .press-block > p { font-size: 15px; color: var(--text-muted); margin-bottom: 16px; }
  .press-email {
    display: inline-block;
    font-size: 18px; font-weight: 700;
    color: var(--brand); text-decoration: none;
    margin-bottom: 28px;
    transition: color 0.15s ease;
  }
  .press-email:hover { color: var(--brand-dark); }
  .press-assets { text-align: center; }
  .assets-label { display: block; font-size: 12px; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.08em; font-weight: 600; margin-bottom: 12px; }
  .assets-tags { display: flex; flex-wrap: wrap; gap: 8px; justify-content: center; }
  .tag {
    background: var(--soft); border: 1px solid var(--border);
    border-radius: 100px; padding: 6px 14px;
    font-size: 12px; color: var(--text-muted); font-weight: 500;
  }

  @media (max-width: 1024px) {
    .media-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 640px) {
    .media-grid { grid-template-columns: 1fr; }
  }
</style>
